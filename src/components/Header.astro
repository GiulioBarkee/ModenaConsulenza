<script>
  let item = document.querySelector(".icon-hamburger");
  item.addEventListener("click", function () {
    document.body.classList.toggle("menu-open");
  });

  window.addEventListener("scroll", function () {
    let header = document.querySelector(".header");
    if (window.scrollY > 300) {
      header.classList.add("scroll-down");
    } else {
      header.classList.remove("scroll-down");
    }
  });

  //sottolineare pagina corrente
  const currentPageUrl = window.location.href;

  const headerLinks =
    document.querySelectorAll<HTMLAnchorElement>(".header__menu li a");

  headerLinks.forEach((link) => {
    if (link.href === currentPageUrl) {
      link.classList.add("current");
    }
  });
</script>
<header class="header">
  <div class="header__content">
    <a class="header__logo" href="/">
      <img class="header__logo" src="/assets/Risorsalogo.svg" alt="" />
    </a>
    <ul class="header__menu">
      <li><a href="/">Home</a></li>
      <li><a href="/chisiamo">Chi siamo</a></li>
      <li><a href="/servizi">Servizi</a></li>
      <li><a href="/contatti">Contatti</a></li>
    </ul>
    <div class="header__quick">
      <a class="button" href="#">Chiamaci ora</a>
      <div class="icon-hamburger">
        <span></span>
        <span></span>
      </div>
    </div>
  </div>
</header>
<style is:global>
  /* Menu */
  :root {
    --menu-bg: #fff;
    --menu-color: #002169;
  }

  .header {
    background-color: var(--menu-bg);
    position: fixed;
    top: 3.5rem;
    left: 50%;
    z-index: 9999;
    width: 86%;
    padding: 15px 20px;
    border-radius: 40px;
    transform: translateX(-50%);
    box-shadow: 0px 4px 18px 0px rgba(0, 0, 0, 0.1);
  
  }
  .header__content {
    width: 100%;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    max-width: calc(1750px - 9rem)
  }
  .scroll-down .header__content{max-width: 1750px;}
  .header .button {
    border-radius: 18px;
    white-space: nowrap;
  }
  .header__logo,
  .header__quick {
    display: flex;
    align-items: center;
    color: var(--menu-color);
    width: 40%;
  }
  .header__logo {
    width: 360px;
    /* padding-left: 0.15rem; */
  }

 
  .scroll-down .header__quick{width: 18%;}

  .header__logo img {
    width: 100%;
    margin-left: 1rem;
  }

  .scroll-down .header__logo img{margin-left: 5.5rem;}
  .header__quick {
    justify-content: flex-end;
    text-align: center;
  }
  
  .header__quick {
    width: 25%;
  }

  .header__menu {
    margin: 0;
    width: 100%;
    display: flex;
    justify-content: flex-end;
  }
  .header__menu li {
    display: flex;
    align-items: center;
  }
  .header__menu li a {
    color: var(--menu-color);
    opacity: 0.8;
    display: block;
    padding: 0 1.6rem;
    font-size: 1.1rem;
    font-weight: 500;
    text-decoration: none;
    white-space: nowrap;
  }

  .header__menu li a.current {
    text-decoration: underline;
    text-underline-offset: 6px!important;
    text-decoration-thickness: 2px!important;
 
  }
  .header__menu li a:hover {
    text-decoration: underline;
    text-underline-offset: 6px !important;
    text-decoration-thickness: 2px !important;
}
    

  @media (max-width: 1000px) {
    .header__menu {
      position: absolute;
      top: 70px;
      left: 0;
      background-color: var(--menu-bg);
      width: 100%;
      height: 100vh;
      height: 0vh;
      overflow: hidden;
      transition:
        height 1s cubic-bezier(0.215, 0.61, 0.355, 1),
        padding 0s; /* Rimuovi la transizione per il padding */
      padding: 0;
    }

    .scroll-down .header__quick{ margin-right: 0!important;}
    .scroll-down .header__logo img{margin-left: 1rem!important ;} 


    .scroll-down .header__quick{width: 50%;}

    .header__menu li a {
      opacity: 0;
    }
    .menu-open .header__menu li a {
      width: 60%;
      border-bottom: 1px solid #444;
      height: 4rem;
      margin: 0 auto;
      display: flex;
      opacity: 1 !important;
      justify-content: center;
      align-items: center;
    }
    .header__menu li a {
      margin: 0 auto;
    }
    .menu-open .header__menu {
      height: 100vh;
      padding: 3% 0;
      display: block;
    }

    .icon-hamburger {
      height: 50px;
      width: 40px;
      margin-left: 20px;
      padding-top: 5px;
    }
    .icon-hamburger span {
      height: 2px;
      width: 30px;
      background: var(--menu-color);
      position: relative;
      display: block;
      margin-top: 11px;
      transition: all 0.2s cubic-bezier(0.215, 0.61, 0.355, 1);
    }

    .menu-open .icon-hamburger span:nth-child(1) {
      transform: rotate(45deg) translateY(9px);
    }
    .menu-open .icon-hamburger span:nth-child(2) {
      transform: rotate(-45deg) translateY(-9px);
    }

    .header__quick {
      display: flex;
      justify-content: flex-end;
      width: 50%;
      padding-right: 1rem;
    }
    .header__quick a {
      display: none;
    }
    .menu-open .header__quick a {
      display: block;
      position: fixed;
      top: 500%;
      left: 50%;
      transform: translateX(-50%);
    }

    .header {
      top: 0px;
      width: 100%;
      border-radius: 0;
    }
  }

  .scroll-down {
    top: 0;
    width: 100%;
    border-radius: 0;
    transition: all 1s cubic-bezier(0.215, 0.61, 0.355, 1);

  }
  .scroll-down .header__quick {
    transition: all 1s cubic-bezier(0.215, 0.61, 0.355, 1);
    margin-right: 5.5rem;
  }
  .header__menu li a.current {
    text-decoration: underline;
  }
</style>
